# Basic server — Bun + TypeScript Edition

## Run

```bash
# Install bun-types (for type checking only, not required to run)
bun install

# Development (hot reload)
bun dev

# Production
bun start
```

Requires Bun v1.0+. Bun runs TypeScript natively — no `tsc`, no build step.
